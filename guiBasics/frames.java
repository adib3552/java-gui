package guiBasics;

import javax.swing.*;
import java.awt.*;

public class frames {
    public static void main(String[] args){
        /*
        //JFrame = a GUI window to add components to
        JFrame frame=new JFrame(); //creates a frame
        frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);//exits out of frame
        frame.setTitle("GUI");//sets title of the frame
        frame.setSize(420,420);//sets the x and y dimension of frame
        frame.setResizable(false);
        frame.setVisible(true);//make frame visible

        ImageIcon image=new ImageIcon("");
        frame.setIconImage(image.getImage());//changes icon of the logo
        frame.getContentPane().setBackground(Color.blue);//changes color of the background
         */
        MyFrame myFrame=new MyFrame();
        //myFrame.button.setBounds();
    }
}
